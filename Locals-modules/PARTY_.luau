-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

-- Bytecode (Base64):
-- 

-- https://lua.expert/
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local v1 = require(ReplicatedStorage:WaitForChild("VisualEnhancers"):WaitForChild("GUI Enhancer"))
local Click = _G.Sounds.Extra.Click
local Buttons = PlayerGui:WaitForChild("Display"):WaitForChild("Frame"):WaitForChild("Buttons")
local MainFrame = PlayerGui:WaitForChild("AllyUI"):WaitForChild("MainFrame")
local v2 = MainFrame:WaitForChild("Content")
local AllyFrame = v2:WaitForChild("AllyFrame")
local CreateAllyScreen = AllyFrame:WaitForChild("CreateAllyScreen")
local Players2 = AllyFrame:WaitForChild("Players")
local LeaveAlly = AllyFrame:WaitForChild("LeaveAlly")
local Users = v2:WaitForChild("UserList"):WaitForChild("Users")
local SearchBox = v2:WaitForChild("Search"):WaitForChild("SearchBox")
local Template = Users:WaitForChild("Template")
local Template2 = Players2:WaitForChild("Template")
local Player = Players2:WaitForChild("Player")
local CloseButton = MainFrame:WaitForChild("CloseButton")
local Create = CreateAllyScreen:WaitForChild("Create")
local t = {
	InviteCd = 0.35,
	CreatePartyCd = 0.5,
	LeavePartyCd = 0.5,
	RespondRequestCd = 0.5,
	CooldownDefault = 0.5,
	UpdateDebounce = 0.05,
	RequestTimeout = 10
}
local v3 = false
local t2 = {}
local t3 = {}

local function isOnCooldown(p1) --[[ isOnCooldown | Line: 55 | Upvalues: t2 (copy) ]]
	local v1 = t2[p1]

	if not v1 then
		return false
	end

	return os.clock() - v1 < (t2[p1 .. "_time"] or 0.5)
end

local function setCooldown(p1, p2) --[[ setCooldown | Line: 61 | Upvalues: t2 (copy) ]]
	t2[p1] = os.clock()
	t2[p1 .. "_time"] = p2
end

local function bindEnhancer(p1, p2, p3) --[[ bindEnhancer | Line: 66 | Upvalues: v1 (copy) ]]
	if p1:GetAttribute("EnhancerBound") then
		return
	end

	p1:SetAttribute("EnhancerBound", true)

	if p3 then
		v1.AddClickEffects(p1)
		v1.AddHoverEffects(p1)

		return
	end

	local v12 = p1:FindFirstChild("Icon", true) or (p1:FindFirstChildWhichIsA("TextLabel", true) or p1)
	local v2 = v12:IsA("ImageLabel") or v12:IsA("ImageButton")

	v1.AddClickEffects(p1, v12, v2, p2 or p1)
	v1.AddHoverEffects(p1, v12, v2, p2 or p1)
end

local function closeUI() --[[ closeUI | Line: 82 | Upvalues: Buttons (copy) ]]
	Buttons:SetAttribute("CurrentOpen", "None")
end

local function clearUserList() --[[ clearUserList | Line: 86 | Upvalues: Users (copy) ]]
	for i, v in ipairs(Users:GetChildren()) do
		if v:IsA("Frame") and (v.Name ~= "EndPadding" and v.Name ~= "Template") then
			v:Destroy()
		end
	end
end

local function clearPartyList() --[[ clearPartyList | Line: 94 | Upvalues: Players2 (copy) ]]
	for i, v in ipairs(Players2:GetChildren()) do
		if v:IsA("ImageButton") and v.Name ~= "Player" then
			v:Destroy()
		end
	end
end

local function getAvatar(p1, p2, p3) --[[ getAvatar | Line: 102 | Upvalues: Players (copy) ]]
	task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), p1 (copy), p3 (copy), p2 (copy) ]]
		local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), p1 (ref) ]]
			return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
		end)

		if not (ok and p3.Parent) then
			return
		end

		p2.Image = result
	end)
end

local function updateParty() --[[ updateParty | Line: 117 | Upvalues: v3 (ref), LocalPlayer (copy), CreateAllyScreen (copy), Players2 (copy), LeaveAlly (copy), clearUserList (copy), clearPartyList (copy), Players (copy), Template2 (copy), Player (copy), bindEnhancer (copy), t2 (copy), t (copy), Click (copy), SearchBox (copy), Template (copy), Users (copy) ]]
	if not v3 then
		v3 = true
		task.spawn(function() --[[ Line: 120 | Upvalues: v3 (ref), LocalPlayer (ref), CreateAllyScreen (ref), Players2 (ref), LeaveAlly (ref), clearUserList (ref), clearPartyList (ref), Players (ref), Template2 (ref), Player (ref), bindEnhancer (ref), t2 (ref), t (ref), Click (ref), SearchBox (ref), Template (ref), Users (ref) ]]
			task.wait(0.05)
			v3 = false

			local v1 = LocalPlayer:GetAttribute("Party") or ""
			local v2 = LocalPlayer:GetAttribute("PartyLeader") == true
			local v32 = if v1 == "" then false else true

			CreateAllyScreen.Visible = not v32
			Players2.Visible = v32
			LeaveAlly.Visible = v32
			clearUserList()
			clearPartyList()

			for i2, v in ipairs(Players:GetPlayers()) do
				local v4 = v:GetAttribute("Party") or ""
				local v5 = if v:GetAttribute("PartyLeader") == true then true else false

				if v32 and v4 == v1 then
					if v5 then
						local Holder = Template2:FindFirstChild("Holder")

						if Holder then
							local Display = Holder:FindFirstChild("Display")
							local Username = Holder:FindFirstChild("Username")

							if Display then
								Display.Text = v.DisplayName
							end

							if Username then
								Username.Text = v.Name
							end
						end

						local PlayerAvatar = Template2:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId
								local v6 = Template2

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v6 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v6.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						continue
					end

					local v7 = Player:Clone()

					v7.Name = "Member_" .. v.Name
					v7.Visible = true

					local Display = v7:FindFirstChild("Display")

					if Display then
						Display.Text = v.DisplayName
					end

					local PlayerAvatar = v7:FindFirstChild("PlayerAvatar")

					if PlayerAvatar then
						local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

						if PlayerAvatar2 then
							local UserId = v.UserId

							task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v7 (copy), PlayerAvatar2 (copy) ]]
								local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
									return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
								end)

								if not (ok and v7.Parent) then
									return
								end

								PlayerAvatar2.Image = result
							end)
						end
					end

					local ButtonHolder = v7:FindFirstChild("ButtonHolder")

					if ButtonHolder then
						local Kick = ButtonHolder:FindFirstChild("Kick")

						if Kick and Kick:IsA("GuiButton") then
							Kick.Visible = v2
							bindEnhancer(Kick)
							Kick.Activated:Connect(function() --[[ Line: 171 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
								if LocalPlayer:GetAttribute("PartyLeader") ~= true then
									return
								end

								local v1 = "Kick_" .. v.Name
								local v2 = t2[v1]

								if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
									Click:Play()
									t2[v1] = os.clock()
									t2[v1 .. "_time"] = 0.5
									_G.InputFunction:InvokeServer("Party", "Kick", v)
								end
							end)
						end
					end

					v7.Parent = Players2

					continue
				end

				if v ~= LocalPlayer and not v5 then
					local v8 = string.lower(SearchBox.Text or "")

					if v8 == "" or (string.find(string.lower(v.Name), v8, 1, true) or string.find(string.lower(v.DisplayName), v8, 1, true)) then
						local v9 = Template:Clone()

						v9.Name = "User_" .. v.Name
						v9.Visible = true

						local Holder = v9:FindFirstChild("Holder")

						if Holder then
							local Username = Holder:FindFirstChild("Username")
							local Display = Holder:FindFirstChild("Display")

							if Username then
								Username.Text = v.Name
							end

							if Display then
								Display.Text = v.DisplayName
							end
						end

						local PlayerAvatar = v9:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v9 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v9.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						local ButtonHolder = v9:FindFirstChild("ButtonHolder")

						if ButtonHolder then
							local Request = ButtonHolder:FindFirstChild("Request")

							if Request and Request:IsA("GuiButton") then
								local v10 = if v32 then if v2 then if v4 == "" then true else false else v2 else v32

								Request.Visible = v10
								Request.ImageTransparency = if v10 then 0 else 0.5
								bindEnhancer(Request, Request)
								Request.Activated:Connect(function() --[[ Line: 218 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
									if LocalPlayer:GetAttribute("Party") == "" then
										return
									end

									if LocalPlayer:GetAttribute("PartyLeader") ~= true then
										return
									end

									local v1 = "Invite_" .. v.Name
									local v2 = t2[v1]

									if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
										Click:Play()
										t2[v1] = os.clock()
										t2[v1 .. "_time"] = 0.35
										_G.InputFunction:InvokeServer("Party", "Invite", v)
									end
								end)
							end
						end

						v9.Parent = Users
					end
				end
			end
		end)
	end
end

local function connectPlayerSignals(p1) --[[ connectPlayerSignals | Line: 235 | Upvalues: t3 (copy), updateParty (copy) ]]
	if not t3[p1] then
		t3[p1] = {
			Party = p1:GetAttributeChangedSignal("Party"):Connect(updateParty),
			PartyLeader = p1:GetAttributeChangedSignal("PartyLeader"):Connect(updateParty)
		}
	end
end

if not CloseButton:GetAttribute("EnhancerBound") then
	CloseButton:SetAttribute("EnhancerBound", true)
	v1.AddClickEffects(CloseButton)
	v1.AddHoverEffects(CloseButton)
end

bindEnhancer(Create, Create)
bindEnhancer(LeaveAlly, LeaveAlly)
Buttons:GetAttributeChangedSignal("CurrentOpen"):Connect(function() --[[ Line: 251 | Upvalues: Buttons (copy), MainFrame (copy), v3 (ref), LocalPlayer (copy), CreateAllyScreen (copy), Players2 (copy), LeaveAlly (copy), clearUserList (copy), clearPartyList (copy), Players (copy), Template2 (copy), Player (copy), bindEnhancer (copy), t2 (copy), t (copy), Click (copy), SearchBox (copy), Template (copy), Users (copy) ]]
	if Buttons:GetAttribute("CurrentOpen") ~= "Party" then
		MainFrame.Visible = false

		return
	end

	MainFrame.Visible = true

	if not v3 then
		v3 = true
		task.spawn(function() --[[ Line: 120 | Upvalues: v3 (ref), LocalPlayer (ref), CreateAllyScreen (ref), Players2 (ref), LeaveAlly (ref), clearUserList (ref), clearPartyList (ref), Players (ref), Template2 (ref), Player (ref), bindEnhancer (ref), t2 (ref), t (ref), Click (ref), SearchBox (ref), Template (ref), Users (ref) ]]
			task.wait(0.05)
			v3 = false

			local v1 = LocalPlayer:GetAttribute("Party") or ""
			local v2 = LocalPlayer:GetAttribute("PartyLeader") == true
			local v32 = if v1 == "" then false else true

			CreateAllyScreen.Visible = not v32
			Players2.Visible = v32
			LeaveAlly.Visible = v32
			clearUserList()
			clearPartyList()

			for i2, v in ipairs(Players:GetPlayers()) do
				local v4 = v:GetAttribute("Party") or ""
				local v5 = if v:GetAttribute("PartyLeader") == true then true else false

				if v32 and v4 == v1 then
					if v5 then
						local Holder = Template2:FindFirstChild("Holder")

						if Holder then
							local Display = Holder:FindFirstChild("Display")
							local Username = Holder:FindFirstChild("Username")

							if Display then
								Display.Text = v.DisplayName
							end

							if Username then
								Username.Text = v.Name
							end
						end

						local PlayerAvatar = Template2:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId
								local v6 = Template2

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v6 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v6.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						continue
					end

					local v7 = Player:Clone()

					v7.Name = "Member_" .. v.Name
					v7.Visible = true

					local Display = v7:FindFirstChild("Display")

					if Display then
						Display.Text = v.DisplayName
					end

					local PlayerAvatar = v7:FindFirstChild("PlayerAvatar")

					if PlayerAvatar then
						local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

						if PlayerAvatar2 then
							local UserId = v.UserId

							task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v7 (copy), PlayerAvatar2 (copy) ]]
								local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
									return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
								end)

								if not (ok and v7.Parent) then
									return
								end

								PlayerAvatar2.Image = result
							end)
						end
					end

					local ButtonHolder = v7:FindFirstChild("ButtonHolder")

					if ButtonHolder then
						local Kick = ButtonHolder:FindFirstChild("Kick")

						if Kick and Kick:IsA("GuiButton") then
							Kick.Visible = v2
							bindEnhancer(Kick)
							Kick.Activated:Connect(function() --[[ Line: 171 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
								if LocalPlayer:GetAttribute("PartyLeader") ~= true then
									return
								end

								local v1 = "Kick_" .. v.Name
								local v2 = t2[v1]

								if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
									Click:Play()
									t2[v1] = os.clock()
									t2[v1 .. "_time"] = 0.5
									_G.InputFunction:InvokeServer("Party", "Kick", v)
								end
							end)
						end
					end

					v7.Parent = Players2

					continue
				end

				if v ~= LocalPlayer and not v5 then
					local v8 = string.lower(SearchBox.Text or "")

					if v8 == "" or (string.find(string.lower(v.Name), v8, 1, true) or string.find(string.lower(v.DisplayName), v8, 1, true)) then
						local v9 = Template:Clone()

						v9.Name = "User_" .. v.Name
						v9.Visible = true

						local Holder = v9:FindFirstChild("Holder")

						if Holder then
							local Username = Holder:FindFirstChild("Username")
							local Display = Holder:FindFirstChild("Display")

							if Username then
								Username.Text = v.Name
							end

							if Display then
								Display.Text = v.DisplayName
							end
						end

						local PlayerAvatar = v9:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v9 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v9.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						local ButtonHolder = v9:FindFirstChild("ButtonHolder")

						if ButtonHolder then
							local Request = ButtonHolder:FindFirstChild("Request")

							if Request and Request:IsA("GuiButton") then
								local v10 = if v32 then if v2 then if v4 == "" then true else false else v2 else v32

								Request.Visible = v10
								Request.ImageTransparency = if v10 then 0 else 0.5
								bindEnhancer(Request, Request)
								Request.Activated:Connect(function() --[[ Line: 218 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
									if LocalPlayer:GetAttribute("Party") == "" then
										return
									end

									if LocalPlayer:GetAttribute("PartyLeader") ~= true then
										return
									end

									local v1 = "Invite_" .. v.Name
									local v2 = t2[v1]

									if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
										Click:Play()
										t2[v1] = os.clock()
										t2[v1 .. "_time"] = 0.35
										_G.InputFunction:InvokeServer("Party", "Invite", v)
									end
								end)
							end
						end

						v9.Parent = Users
					end
				end
			end
		end)
	end
end)
CloseButton.Activated:Connect(function() --[[ Line: 263 | Upvalues: Click (copy), Buttons (copy) ]]
	Click:Play()
	Buttons:SetAttribute("CurrentOpen", "None")
end)
Create.Activated:Connect(function() --[[ Line: 268 | Upvalues: t2 (copy), t (copy), Click (copy) ]]
	local CreateParty = t2.CreateParty
	local v1

	if CreateParty then
		local v2 = os.clock() - CreateParty

		v1 = v2 < (t2.CreateParty_time or t.CooldownDefault)
	else
		v1 = false
	end

	if not v1 then
		Click:Play()
		t2.CreateParty = os.clock()
		t2.CreateParty_time = 0.5
		_G.InputFunction:InvokeServer("Party", "Create")
	end
end)
LeaveAlly.Activated:Connect(function() --[[ Line: 275 | Upvalues: t2 (copy), t (copy), Click (copy) ]]
	local LeaveParty = t2.LeaveParty
	local v1

	if LeaveParty then
		local v2 = os.clock() - LeaveParty

		v1 = v2 < (t2.LeaveParty_time or t.CooldownDefault)
	else
		v1 = false
	end

	if not v1 then
		Click:Play()
		t2.LeaveParty = os.clock()
		t2.LeaveParty_time = 0.5
		_G.InputFunction:InvokeServer("Party", "Leave")
	end
end)
SearchBox:GetPropertyChangedSignal("Text"):Connect(updateParty)
Players.PlayerAdded:Connect(function(p1) --[[ Line: 288 | Upvalues: connectPlayerSignals (copy), v3 (ref), LocalPlayer (copy), CreateAllyScreen (copy), Players2 (copy), LeaveAlly (copy), clearUserList (copy), clearPartyList (copy), Players (copy), Template2 (copy), Player (copy), bindEnhancer (copy), t2 (copy), t (copy), Click (copy), SearchBox (copy), Template (copy), Users (copy) ]]
	repeat
		task.wait()
	until p1:GetAttribute("Loaded") and p1:FindFirstChild("Data")

	connectPlayerSignals(p1)

	if not v3 then
		v3 = true
		task.spawn(function() --[[ Line: 120 | Upvalues: v3 (ref), LocalPlayer (ref), CreateAllyScreen (ref), Players2 (ref), LeaveAlly (ref), clearUserList (ref), clearPartyList (ref), Players (ref), Template2 (ref), Player (ref), bindEnhancer (ref), t2 (ref), t (ref), Click (ref), SearchBox (ref), Template (ref), Users (ref) ]]
			task.wait(0.05)
			v3 = false

			local v1 = LocalPlayer:GetAttribute("Party") or ""
			local v2 = LocalPlayer:GetAttribute("PartyLeader") == true
			local v32 = if v1 == "" then false else true

			CreateAllyScreen.Visible = not v32
			Players2.Visible = v32
			LeaveAlly.Visible = v32
			clearUserList()
			clearPartyList()

			for i2, v in ipairs(Players:GetPlayers()) do
				local v4 = v:GetAttribute("Party") or ""
				local v5 = if v:GetAttribute("PartyLeader") == true then true else false

				if v32 and v4 == v1 then
					if v5 then
						local Holder = Template2:FindFirstChild("Holder")

						if Holder then
							local Display = Holder:FindFirstChild("Display")
							local Username = Holder:FindFirstChild("Username")

							if Display then
								Display.Text = v.DisplayName
							end

							if Username then
								Username.Text = v.Name
							end
						end

						local PlayerAvatar = Template2:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId
								local v6 = Template2

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v6 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v6.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						continue
					end

					local v7 = Player:Clone()

					v7.Name = "Member_" .. v.Name
					v7.Visible = true

					local Display = v7:FindFirstChild("Display")

					if Display then
						Display.Text = v.DisplayName
					end

					local PlayerAvatar = v7:FindFirstChild("PlayerAvatar")

					if PlayerAvatar then
						local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

						if PlayerAvatar2 then
							local UserId = v.UserId

							task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v7 (copy), PlayerAvatar2 (copy) ]]
								local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
									return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
								end)

								if not (ok and v7.Parent) then
									return
								end

								PlayerAvatar2.Image = result
							end)
						end
					end

					local ButtonHolder = v7:FindFirstChild("ButtonHolder")

					if ButtonHolder then
						local Kick = ButtonHolder:FindFirstChild("Kick")

						if Kick and Kick:IsA("GuiButton") then
							Kick.Visible = v2
							bindEnhancer(Kick)
							Kick.Activated:Connect(function() --[[ Line: 171 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
								if LocalPlayer:GetAttribute("PartyLeader") ~= true then
									return
								end

								local v1 = "Kick_" .. v.Name
								local v2 = t2[v1]

								if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
									Click:Play()
									t2[v1] = os.clock()
									t2[v1 .. "_time"] = 0.5
									_G.InputFunction:InvokeServer("Party", "Kick", v)
								end
							end)
						end
					end

					v7.Parent = Players2

					continue
				end

				if v ~= LocalPlayer and not v5 then
					local v8 = string.lower(SearchBox.Text or "")

					if v8 == "" or (string.find(string.lower(v.Name), v8, 1, true) or string.find(string.lower(v.DisplayName), v8, 1, true)) then
						local v9 = Template:Clone()

						v9.Name = "User_" .. v.Name
						v9.Visible = true

						local Holder = v9:FindFirstChild("Holder")

						if Holder then
							local Username = Holder:FindFirstChild("Username")
							local Display = Holder:FindFirstChild("Display")

							if Username then
								Username.Text = v.Name
							end

							if Display then
								Display.Text = v.DisplayName
							end
						end

						local PlayerAvatar = v9:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v9 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v9.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						local ButtonHolder = v9:FindFirstChild("ButtonHolder")

						if ButtonHolder then
							local Request = ButtonHolder:FindFirstChild("Request")

							if Request and Request:IsA("GuiButton") then
								local v10 = if v32 then if v2 then if v4 == "" then true else false else v2 else v32

								Request.Visible = v10
								Request.ImageTransparency = if v10 then 0 else 0.5
								bindEnhancer(Request, Request)
								Request.Activated:Connect(function() --[[ Line: 218 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
									if LocalPlayer:GetAttribute("Party") == "" then
										return
									end

									if LocalPlayer:GetAttribute("PartyLeader") ~= true then
										return
									end

									local v1 = "Invite_" .. v.Name
									local v2 = t2[v1]

									if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
										Click:Play()
										t2[v1] = os.clock()
										t2[v1 .. "_time"] = 0.35
										_G.InputFunction:InvokeServer("Party", "Invite", v)
									end
								end)
							end
						end

						v9.Parent = Users
					end
				end
			end
		end)
	end
end)
Players.PlayerRemoving:Connect(function(p1) --[[ Line: 294 | Upvalues: t3 (copy), v3 (ref), LocalPlayer (copy), CreateAllyScreen (copy), Players2 (copy), LeaveAlly (copy), clearUserList (copy), clearPartyList (copy), Players (copy), Template2 (copy), Player (copy), bindEnhancer (copy), t2 (copy), t (copy), Click (copy), SearchBox (copy), Template (copy), Users (copy) ]]
	if t3[p1] then
		for k, v in pairs(t3[p1]) do
			v:Disconnect()
		end

		t3[p1] = nil
	end

	task.wait(0.1)

	if not v3 then
		v3 = true
		task.spawn(function() --[[ Line: 120 | Upvalues: v3 (ref), LocalPlayer (ref), CreateAllyScreen (ref), Players2 (ref), LeaveAlly (ref), clearUserList (ref), clearPartyList (ref), Players (ref), Template2 (ref), Player (ref), bindEnhancer (ref), t2 (ref), t (ref), Click (ref), SearchBox (ref), Template (ref), Users (ref) ]]
			task.wait(0.05)
			v3 = false

			local v1 = LocalPlayer:GetAttribute("Party") or ""
			local v2 = LocalPlayer:GetAttribute("PartyLeader") == true
			local v32 = if v1 == "" then false else true

			CreateAllyScreen.Visible = not v32
			Players2.Visible = v32
			LeaveAlly.Visible = v32
			clearUserList()
			clearPartyList()

			for i2, v in ipairs(Players:GetPlayers()) do
				local v4 = v:GetAttribute("Party") or ""
				local v5 = if v:GetAttribute("PartyLeader") == true then true else false

				if v32 and v4 == v1 then
					if v5 then
						local Holder = Template2:FindFirstChild("Holder")

						if Holder then
							local Display = Holder:FindFirstChild("Display")
							local Username = Holder:FindFirstChild("Username")

							if Display then
								Display.Text = v.DisplayName
							end

							if Username then
								Username.Text = v.Name
							end
						end

						local PlayerAvatar = Template2:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId
								local v6 = Template2

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v6 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v6.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						continue
					end

					local v7 = Player:Clone()

					v7.Name = "Member_" .. v.Name
					v7.Visible = true

					local Display = v7:FindFirstChild("Display")

					if Display then
						Display.Text = v.DisplayName
					end

					local PlayerAvatar = v7:FindFirstChild("PlayerAvatar")

					if PlayerAvatar then
						local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

						if PlayerAvatar2 then
							local UserId = v.UserId

							task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v7 (copy), PlayerAvatar2 (copy) ]]
								local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
									return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
								end)

								if not (ok and v7.Parent) then
									return
								end

								PlayerAvatar2.Image = result
							end)
						end
					end

					local ButtonHolder = v7:FindFirstChild("ButtonHolder")

					if ButtonHolder then
						local Kick = ButtonHolder:FindFirstChild("Kick")

						if Kick and Kick:IsA("GuiButton") then
							Kick.Visible = v2
							bindEnhancer(Kick)
							Kick.Activated:Connect(function() --[[ Line: 171 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
								if LocalPlayer:GetAttribute("PartyLeader") ~= true then
									return
								end

								local v1 = "Kick_" .. v.Name
								local v2 = t2[v1]

								if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
									Click:Play()
									t2[v1] = os.clock()
									t2[v1 .. "_time"] = 0.5
									_G.InputFunction:InvokeServer("Party", "Kick", v)
								end
							end)
						end
					end

					v7.Parent = Players2

					continue
				end

				if v ~= LocalPlayer and not v5 then
					local v8 = string.lower(SearchBox.Text or "")

					if v8 == "" or (string.find(string.lower(v.Name), v8, 1, true) or string.find(string.lower(v.DisplayName), v8, 1, true)) then
						local v9 = Template:Clone()

						v9.Name = "User_" .. v.Name
						v9.Visible = true

						local Holder = v9:FindFirstChild("Holder")

						if Holder then
							local Username = Holder:FindFirstChild("Username")
							local Display = Holder:FindFirstChild("Display")

							if Username then
								Username.Text = v.Name
							end

							if Display then
								Display.Text = v.DisplayName
							end
						end

						local PlayerAvatar = v9:FindFirstChild("PlayerAvatar")

						if PlayerAvatar then
							local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

							if PlayerAvatar2 then
								local UserId = v.UserId

								task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v9 (copy), PlayerAvatar2 (copy) ]]
									local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
										return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
									end)

									if not (ok and v9.Parent) then
										return
									end

									PlayerAvatar2.Image = result
								end)
							end
						end

						local ButtonHolder = v9:FindFirstChild("ButtonHolder")

						if ButtonHolder then
							local Request = ButtonHolder:FindFirstChild("Request")

							if Request and Request:IsA("GuiButton") then
								local v10 = if v32 then if v2 then if v4 == "" then true else false else v2 else v32

								Request.Visible = v10
								Request.ImageTransparency = if v10 then 0 else 0.5
								bindEnhancer(Request, Request)
								Request.Activated:Connect(function() --[[ Line: 218 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
									if LocalPlayer:GetAttribute("Party") == "" then
										return
									end

									if LocalPlayer:GetAttribute("PartyLeader") ~= true then
										return
									end

									local v1 = "Invite_" .. v.Name
									local v2 = t2[v1]

									if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
										Click:Play()
										t2[v1] = os.clock()
										t2[v1 .. "_time"] = 0.35
										_G.InputFunction:InvokeServer("Party", "Invite", v)
									end
								end)
							end
						end

						v9.Parent = Users
					end
				end
			end
		end)
	end
end)

for i, v in ipairs(Players:GetPlayers()) do
	connectPlayerSignals(v)
end

connectPlayerSignals(LocalPlayer)
PlayerGui.ChildAdded:Connect(function(p1) --[[ Line: 313 | Upvalues: TweenService (copy), bindEnhancer (copy), Click (copy) ]]
	if p1.Name == "Invite" then
		task.defer(function() --[[ Line: 315 | Upvalues: p1 (copy), TweenService (ref), bindEnhancer (ref), Click (ref) ]]
			local Name = p1:FindFirstChild("Name")
			local Frame = p1:WaitForChild("Frame")
			local v1 = if Name then Name.Value or "Someone" else "Someone"
			local v2 = false

			Frame.Size = UDim2.new(0.0416, 0, 0.0298, 0)
			TweenService:Create(Frame, TweenInfo.new(0.1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
				Size = UDim2.new(0.208, 0, 0.149, 0)
			}):Play()

			local ButtonsHolder = Frame:WaitForChild("ButtonsHolder")
			local Yes = ButtonsHolder:WaitForChild("Yes")
			local No = ButtonsHolder:WaitForChild("No")

			bindEnhancer(Yes)
			bindEnhancer(No)

			local function dismiss() --[[ dismiss | Line: 336 | Upvalues: Click (ref), TweenService (ref), Frame (copy), p1 (ref) ]]
				Click:Play()
				TweenService:Create(Frame, TweenInfo.new(0.1, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
					Size = UDim2.new(0, 0, 0, 0)
				}):Play()
				task.defer(function() --[[ Line: 343 | Upvalues: p1 (ref) ]]
					task.wait(0.15)

					if not (p1 and p1.Parent) then
						return
					end

					p1:Destroy()
				end)
			end

			Yes.MouseButton1Up:Connect(function() --[[ Line: 349 | Upvalues: v2 (ref), dismiss (copy) ]]
				if not v2 then
					v2 = true
					dismiss()
					_G.InputFunction:InvokeServer("Party", "InviteRespond", true)
				end
			end)
			No.MouseButton1Up:Connect(function() --[[ Line: 355 | Upvalues: v2 (ref), dismiss (copy) ]]
				if not v2 then
					v2 = true
					dismiss()
					_G.InputFunction:InvokeServer("Party", "InviteRespond", false)
				end
			end)

			local InfoHolder = Frame:FindFirstChild("InfoHolder")
			local v3 = if InfoHolder then InfoHolder:FindFirstChild("Txt") else InfoHolder

			if v3 then
				local v4 = "<font color=\"rgb(255, 200, 0)\">" .. v1 .. "</font> has invited you to the party."

				v3.RichText = true
				v3.MaxVisibleGraphemes = 0
				v3.Text = v4

				for v5 in utf8.graphemes(v4) do
					if v3.MaxVisibleGraphemes == -1 then
						break
					end

					local v6 = string.sub(v4, v5, v5)

					v3.MaxVisibleGraphemes = v3.MaxVisibleGraphemes + 1

					if v6 ~= " " then
						task.wait(0.01)
					end
				end
			end

			task.spawn(function() --[[ Line: 377 | Upvalues: v2 (ref), dismiss (copy) ]]
				task.wait(15)

				if not v2 then
					v2 = true
					dismiss()
					_G.InputFunction:InvokeServer("Party", "InviteRespond", false)
				end
			end)
		end)
	end
end)
MainFrame.Visible = false

if v3 then
	return {}
end

v3 = true
task.spawn(function() --[[ Line: 120 | Upvalues: v3 (ref), LocalPlayer (copy), CreateAllyScreen (copy), Players2 (copy), LeaveAlly (copy), clearUserList (copy), clearPartyList (copy), Players (copy), Template2 (copy), Player (copy), bindEnhancer (copy), t2 (copy), t (copy), Click (copy), SearchBox (copy), Template (copy), Users (copy) ]]
	task.wait(0.05)
	v3 = false

	local v1 = LocalPlayer:GetAttribute("Party") or ""
	local v2 = LocalPlayer:GetAttribute("PartyLeader") == true
	local v32 = if v1 == "" then false else true

	CreateAllyScreen.Visible = not v32
	Players2.Visible = v32
	LeaveAlly.Visible = v32
	clearUserList()
	clearPartyList()

	for i2, v in ipairs(Players:GetPlayers()) do
		local v4 = v:GetAttribute("Party") or ""
		local v5 = if v:GetAttribute("PartyLeader") == true then true else false

		if v32 and v4 == v1 then
			if v5 then
				local Holder = Template2:FindFirstChild("Holder")

				if Holder then
					local Display = Holder:FindFirstChild("Display")
					local Username = Holder:FindFirstChild("Username")

					if Display then
						Display.Text = v.DisplayName
					end

					if Username then
						Username.Text = v.Name
					end
				end

				local PlayerAvatar = Template2:FindFirstChild("PlayerAvatar")

				if PlayerAvatar then
					local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

					if PlayerAvatar2 then
						local UserId = v.UserId
						local v6 = Template2

						task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v6 (copy), PlayerAvatar2 (copy) ]]
							local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
								return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
							end)

							if not (ok and v6.Parent) then
								return
							end

							PlayerAvatar2.Image = result
						end)
					end
				end

				continue
			end

			local v7 = Player:Clone()

			v7.Name = "Member_" .. v.Name
			v7.Visible = true

			local Display = v7:FindFirstChild("Display")

			if Display then
				Display.Text = v.DisplayName
			end

			local PlayerAvatar = v7:FindFirstChild("PlayerAvatar")

			if PlayerAvatar then
				local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

				if PlayerAvatar2 then
					local UserId = v.UserId

					task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v7 (copy), PlayerAvatar2 (copy) ]]
						local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
							return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
						end)

						if not (ok and v7.Parent) then
							return
						end

						PlayerAvatar2.Image = result
					end)
				end
			end

			local ButtonHolder = v7:FindFirstChild("ButtonHolder")

			if ButtonHolder then
				local Kick = ButtonHolder:FindFirstChild("Kick")

				if Kick and Kick:IsA("GuiButton") then
					Kick.Visible = v2
					bindEnhancer(Kick)
					Kick.Activated:Connect(function() --[[ Line: 171 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
						if LocalPlayer:GetAttribute("PartyLeader") ~= true then
							return
						end

						local v1 = "Kick_" .. v.Name
						local v2 = t2[v1]

						if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
							Click:Play()
							t2[v1] = os.clock()
							t2[v1 .. "_time"] = 0.5
							_G.InputFunction:InvokeServer("Party", "Kick", v)
						end
					end)
				end
			end

			v7.Parent = Players2

			continue
		end

		if v ~= LocalPlayer and not v5 then
			local v8 = string.lower(SearchBox.Text or "")

			if v8 == "" or (string.find(string.lower(v.Name), v8, 1, true) or string.find(string.lower(v.DisplayName), v8, 1, true)) then
				local v9 = Template:Clone()

				v9.Name = "User_" .. v.Name
				v9.Visible = true

				local Holder = v9:FindFirstChild("Holder")

				if Holder then
					local Username = Holder:FindFirstChild("Username")
					local Display = Holder:FindFirstChild("Display")

					if Username then
						Username.Text = v.Name
					end

					if Display then
						Display.Text = v.DisplayName
					end
				end

				local PlayerAvatar = v9:FindFirstChild("PlayerAvatar")

				if PlayerAvatar then
					local PlayerAvatar2 = PlayerAvatar:FindFirstChild("PlayerAvatar")

					if PlayerAvatar2 then
						local UserId = v.UserId

						task.spawn(function() --[[ Line: 103 | Upvalues: Players (ref), UserId (copy), v9 (copy), PlayerAvatar2 (copy) ]]
							local ok, result = pcall(function() --[[ Line: 104 | Upvalues: Players (ref), UserId (ref) ]]
								return Players:GetUserThumbnailAsync(p1, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size100x100)
							end)

							if not (ok and v9.Parent) then
								return
							end

							PlayerAvatar2.Image = result
						end)
					end
				end

				local ButtonHolder = v9:FindFirstChild("ButtonHolder")

				if ButtonHolder then
					local Request = ButtonHolder:FindFirstChild("Request")

					if Request and Request:IsA("GuiButton") then
						local v10 = if v32 then if v2 then if v4 == "" then true else false else v2 else v32

						Request.Visible = v10
						Request.ImageTransparency = if v10 then 0 else 0.5
						bindEnhancer(Request, Request)
						Request.Activated:Connect(function() --[[ Line: 218 | Upvalues: LocalPlayer (ref), v (copy), t2 (ref), t (ref), Click (ref) ]]
							if LocalPlayer:GetAttribute("Party") == "" then
								return
							end

							if LocalPlayer:GetAttribute("PartyLeader") ~= true then
								return
							end

							local v1 = "Invite_" .. v.Name
							local v2 = t2[v1]

							if not (if v2 then if os.clock() - v2 < (t2[v1 .. "_time"] or t.CooldownDefault) then true else false else false) then
								Click:Play()
								t2[v1] = os.clock()
								t2[v1 .. "_time"] = 0.35
								_G.InputFunction:InvokeServer("Party", "Invite", v)
							end
						end)
					end
				end

				v9.Parent = Users
			end
		end
	end
end)

return {}